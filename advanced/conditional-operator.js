// const myAge = 17;
// let message;

// // if(myAge >= 18) {
// //   message = 'You can vote';
// // } else {
// //   message = 'You cannot vote';
// // }

// message = myAge >= 18 ? 'You can vote' : 'You cannot vote';

// console.log(message);

const myAge = 3;
const showPage = () => {
  return ('Showing the page');
}
const showErrorPage = () => {
  return ('Showing the error page');
}



console.log(myAge >= 21 ? true  : false);

const team = ['Tyler' , 'Porter' , 'Uganda' , 'Quami' ];
team.length <= 4 ? console.log(`Team size: ${team.length}`) : console.log('Too many people on your team!');

